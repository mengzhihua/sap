services:
  mysql:
    image: mysql:8
    container_name: sap-mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: sap
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - sap_mysql_data:/var/lib/mysql
volumes:
  sap_mysql_data:
