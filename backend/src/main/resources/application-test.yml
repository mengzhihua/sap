spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:sap_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  sql:
    init:
      mode: always
      continue-on-error: false
  h2:
    console:
      enabled: false
